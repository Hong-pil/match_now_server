FROM node:20-alpine

# 작업 디렉토리 설정 (docker-compose.yml과 동일하게)
WORKDIR /usr/src/app

# package.json 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

# 소스코드 복사
COPY . .

# 포트 노출
EXPOSE 3701

# 개발 모드로 실행
CMD ["npm", "run", "start:dev"]